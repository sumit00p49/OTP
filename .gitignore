# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Env & secrets
.env

# Database
*.db
*.sqlite
*.sqlite3
bot_database.db*

# Logs
*.log

# NOTE: products.json IS tracked in git — manage your countries by editing
# it here (or on GitHub). On the server, `git pull` will bring your changes.
# The bot never overwrites it (absolute path + no auto-reset).
